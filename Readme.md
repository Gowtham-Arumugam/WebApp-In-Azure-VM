# web app in azure Virtual machine
1. Terraform to create virtual machine in Azure
2. Assign DNS record in Cloudflare for `webapp.gowtham.living` with the created public IP in Azure
3. Ansible code to configure nginx and copy required files to virtual machine